export const interviews = [
    ['key-role', 'design', 'A PRIMARY KEY uses (OrderID, ProductID). Which statement is correct?', ['Each column must be unique alone', 'The pair is unique and both columns are non-NULL', 'The table has two primary keys', 'The primary key must be nonclustered'], 1, 'One primary-key constraint can cover multiple columns. The whole combination, not each individual value, must be unique.', 'composite-key'],
    ['fk-index', 'performance', 'What does SQL Server automatically create for a foreign key?', ['A clustered index on the child', 'A nonclustered index on the child', 'No child-side index just because the FK exists', 'A materialized join'], 2, 'An FK enforces integrity. A child index is a separate design choice.', 'foreign-key'],
    ['null-check', 'foundations', 'Which condition correctly finds missing Email values?', ['Email = NULL', 'Email <> NULL', 'Email IS NULL', "Email = 'NULL'"], 2, 'NULL comparisons yield UNKNOWN. IS NULL is the absence predicate.', 'null'],
    ['null-anti', 'joins', 'A NOT IN subquery contains a NULL. Why may no nonmatching rows appear?', ['NOT IN sorts incorrectly', 'Nonmatches can evaluate to UNKNOWN', 'The NULL acts as zero', 'The optimizer deletes results'], 1, 'A NULL can make the exclusion UNKNOWN, which WHERE does not retain.', 'not-exists'],
    ['outer-filter', 'joins', 'All customers must remain, but only paid orders may attach. Where should the paid-status predicate go?', ['In the JOIN ON predicate', 'Always in WHERE', 'In ORDER BY', 'In DISTINCT'], 0, 'Filtering optional matches in ON preserves customers with no qualifying paid order.', 'outer-join-filter'],
    ['fanout', 'joins', 'A join key occurs 3 times on one side and 4 on the other. How many matching pairs can that key produce?', ['3', '4', '7', '12'], 3, 'A many-to-many matching key produces 3 x 4 pairs.', 'inner-join'],
    ['count-child', 'aggregation', 'After a LEFT JOIN, how do you count zero orders for a customer with no match?', ['COUNT(*)', 'COUNT(orders.OrderID), where that child key is non-nullable', 'COUNT(customer.CustomerID)', 'SUM(customer.CustomerID)'], 1, 'The preserved parent row makes COUNT(*) equal 1; the NULL-extended child key contributes zero to COUNT(child key).', 'aggregates'],
    ['rank-ties', 'aggregation', 'What does DENSE_RANK return for ordered values 100,100,90?', ['1,2,3', '1,1,3', '1,1,2', '0,0,1'], 2, 'DENSE_RANK shares ties without gaps.', 'ranking'],
    ['windows-grain', 'aggregation', 'How does a SUM window differ from GROUP BY?', ['Windows always sort output', 'Windows retain row detail', 'Windows never use memory', 'GROUP BY cannot use SUM'], 1, 'A window attaches a calculation to rows; grouping changes the output grain.', 'window-functions'],
    ['window-where', 'aggregation', 'How do you filter on ROW_NUMBER in T-SQL?', ['Put it directly in WHERE', 'Use a CTE/derived table then filter its alias', 'Use NOLOCK', 'Replace it with COUNT(*)'], 1, 'Window expressions are not available at the logical WHERE stage of their own SELECT.', 'top-per-group'],
    ['last-value', 'aggregation', 'Why may LAST_VALUE not return the final row of the partition?', ['It always ignores ORDER BY', 'The default frame may end at the current peer group', 'It uses MAX automatically', 'It only works on dates'], 1, 'Specify a frame ending at UNBOUNDED FOLLOWING for the partition-final value.', 'lag-lead'],
    ['cte-cache', 'reuse', 'Which is guaranteed by a normal CTE?', ['The result is stored in tempdb once', 'It is cached across statements', 'Its name is available to the next statement', 'It runs faster than a derived table'], 2, 'A CTE supplies statement scope and readability, not guaranteed materialization or speed.', 'cte'],
    ['view-params', 'reuse', 'Which is a composable parameterized relation in T-SQL?', ['An ordinary view', 'An inline TVF', 'An EXEC result without staging', 'A schema'], 1, 'An inline TVF takes parameters and appears in FROM or APPLY.', 'inline-tvf'],
    ['udf-writes', 'reuse', 'Which routine is appropriate for a transaction that writes several permanent tables?', ['Scalar UDF', 'Inline TVF', 'Stored procedure', 'Ordinary view definition'], 2, 'A procedure can orchestrate side effects and transaction/error handling; UDFs have restrictions.', 'stored-procedure'],
    ['truncate-rollback', 'changes', 'Which statement about SQL Server TRUNCATE is true?', ['It is unlogged', 'It fires DELETE triggers', 'It can roll back within an active transaction', 'It supports WHERE'], 2, 'TRUNCATE is allocation-logged and transactional in SQL Server, with its own restrictions.', 'truncate-drop'],
    ['identity-gaps', 'design', 'What should you assume about IDENTITY numbers?', ['They are gapless', 'They are transaction commit timestamps', 'Gaps are possible', 'They replace UNIQUE constraints'], 2, 'Generation is not a gapless business numbering guarantee, and uniqueness needs a constraint.', 'identity-sequence'],
    ['pk-cluster', 'performance', 'Must a SQL Server primary key be the clustered index?', ['Yes always', 'No; it can be nonclustered', 'Only if composite', 'Only if integer'], 1, 'The PK is logical integrity; clustering is physical organization.', 'clustered-index'],
    ['index-scan', 'performance', 'An actual plan contains an index scan. What is the best response?', ['Force a seek immediately', 'Drop the index', 'Assess rows, coverage, IO and cost before deciding', 'Add NOLOCK'], 2, 'Scanning a small covering index or many qualifying rows can be optimal.', 'execution-plan'],
    ['include', 'performance', 'Why use INCLUDE columns?', ['To make them all navigation keys', 'To cover query payload without widening the search key', 'To force serial execution', 'To remove writes'], 1, 'Included columns supply leaf payload; keys drive search/order.', 'nonclustered-index'],
    ['param-plan', 'performance', 'A routine is slow only for some parameter values. What should be investigated?', ['Only syntax formatting', 'Data skew, estimates, and cached plans', 'Whether SELECT is uppercase', 'Whether the table has a name'], 1, 'Parameter distributions and reuse can create sensitivity; measure representative calls.', 'parameter-plans'],
    ['deadlock-cycle', 'transactions', 'How does a deadlock differ from blocking?', ['They are identical', 'A deadlock is a cycle of waits', 'Blocking always aborts both sessions', 'Deadlocks require a SELECT'], 1, 'Blocking can resolve when the holder finishes; a deadlock cycle needs intervention, typically victim selection.', 'deadlock'],
    ['nolock-safety', 'transactions', 'Which is a valid description of NOLOCK?', ['No locks of any kind', 'Always correct but faster', 'Dirty-read semantics with possible missing/duplicated observations', 'A replacement for indexes'], 2, 'NOLOCK trades correctness and still can use schema-stability locks.', 'nolock'],
    ['snapshot-scope', 'transactions', 'What is the key RCSI versus SNAPSHOT distinction?', ['Both always show the same transaction snapshot', 'RCSI is statement-scoped; SNAPSHOT is transaction-scoped', 'SNAPSHOT uses no resources', 'RCSI permits dirty reads'], 1, 'RCSI provides committed versions for each statement; SNAPSHOT holds a transaction-level view.', 'snapshot'],
    ['nested-commit', 'transactions', 'What does an inner COMMIT normally do in nested T-SQL transactions?', ['Makes its work independently durable', 'Decrements @@TRANCOUNT', 'Rolls back the outer transaction', 'Creates a backup'], 1, 'Only the outer commit completes the unit; nested BEGIN/COMMIT is not independent transaction durability.', 'savepoints'],
    ['injection', 'programmability', 'How should user values enter dynamic SQL?', ['String concatenation', 'A parameter to sp_executesql', 'QUOTENAME applied to the entire SQL', 'Removing every space'], 1, 'Parameterize values; allowlist and quote identifiers separately.', 'parameters-dynamic'],
    ['trigger-rowset', 'programmability', 'What can inserted contain in a SQL Server trigger?', ['Exactly one row', 'A rowset of zero, one, or many rows', 'Only string columns', 'Only the primary key'], 1, 'Triggers run per statement, and must handle set-valued inserted/deleted data.', 'triggers'],
    ['return-output', 'programmability', 'Which procedure mechanism returns an arbitrary result table?', ['RETURN 0', 'A SELECT result set', 'Only a scalar OUTPUT parameter', 'SET NOCOUNT ON'], 1, 'SELECT emits rowsets; RETURN is an integer status; OUTPUT parameters supply scalar values.', 'proc-results'],
    ['date-end', 'query', 'Which filter safely includes all timestamps in January?', ["OrderDate BETWEEN '20260101' AND '20260131'", "OrderDate >= '20260101' AND OrderDate < '20260201'", "OrderDate = 'January'", 'YEAR(OrderDate) > 0'], 1, 'A half-open range includes the complete month without assuming precision at its final day.', 'date-ranges'],
    ['integer-div', 'query', 'What is 5 / 2 when both operands are integers in T-SQL?', ['2.5', '3', '2', 'NULL'], 2, 'Convert at least one operand to decimal before division to preserve a fraction.', 'cast-convert'],
    ['revoke-deny', 'production', 'What does REVOKE do?', ['Explicitly deny every inherited permission', 'Remove a grant or deny at the specified scope', 'Delete the user', 'Drop the role'], 1, 'REVOKE removes that permission entry; effective access can still come from another membership.', 'security'],
    ['backup-proof', 'production', 'What best establishes backup recoverability?', ['Backup filename exists', 'The file is large', 'A tested restore meeting recovery objectives', 'The app starts today'], 2, 'Restoration exercises validate the chain, permissions, media, and process; VERIFYONLY is not the full exercise.', 'backup-recovery'],
    ['sum-distinct', 'patterns', 'Why is SUM(DISTINCT OrderTotal) not a safe general fix for join fanout?', ['SUM never accepts DISTINCT', 'Separate valid orders can share the same total', 'DISTINCT always sorts incorrectly', 'It creates NULL orders'], 1, 'It can discard legitimate equal amounts. Fix grain and cardinality instead.', 'grain-fanout']
].map(([id, module, question, options, correct, explanation, concept]) => ({ id, module, question, options, correct, explanation, concept }));